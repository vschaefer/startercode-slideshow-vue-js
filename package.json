{
  "name": "Cranach Website Demo",
  "version": "1.0.0",
  "description": "Relaunch of the Cranach website with a focus on HTML and CSS.",
  "main": "index.js",
  "scripts": {
    "lint": "stylelint styles/**/*.css",
    "lint:fix": "stylelint styles/**/*.css --fix",
    "dev": "npx browser-sync start --server --files './**/*.html,./**/*.css,./**/*.js'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Christian Noss",
  "license": "MIT",
  "devDependencies": {
    "browsersync": "^0.0.1-security",
    "stylelint": "^16.21.0",
    "stylelint-config-idiomatic-order": "^10.0.0",
    "stylelint-config-standard": "^38.0.0",
    "stylelint-order": "^7.0.0"
  }
}
